---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-dashboard
  namespace: quantum-nlp
  labels:
    app: admin-dashboard
    component: frontend
    tier: admin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-dashboard
  template:
    metadata:
      labels:
        app: admin-dashboard
        component: frontend
        tier: admin
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
      - name: admin-dashboard
        image: ghcr.io/quantumlayerplatform-nlp/quantum-nlp-admin-dashboard:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: NEXT_PUBLIC_API_URL
          value: "http://nlp-gateway:8080"
        - name: NEXT_PUBLIC_WS_URL
          value: "ws://nlp-gateway:8080"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard
  namespace: quantum-nlp
  labels:
    app: admin-dashboard
    component: frontend
spec:
  selector:
    app: admin-dashboard
  ports:
  - port: 3001
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: admin-dashboard-ingress
  namespace: quantum-nlp
  labels:
    app: admin-dashboard
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  rules:
  - host: admin.quantum-nlp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: admin-dashboard
            port:
              number: 3001